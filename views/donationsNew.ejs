<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="/css/style.css">

  <meta charset="UTF-8">
  <title>Add Donation</title>
</head>
<body>

<h1>Add Donation</h1>
<p><a href="/donations">Back to Donations</a></p>

<% if (error_message) { %>
  <p style="color:red;"><%= error_message %></p>
<% } %>

<form method="post" action="/donations/new">
  <input type="hidden" name="_csrf" value="<%= csrfToken %>">

  <% if (isManager) { %>
    <div>
      <label>
        Participant Email:
        <input type="email" name="email" value="<%= email || '' %>" required>
      </label>
    </div>
  <% } else { %>
    <p>Adding donation for your account.</p>
  <% } %>

  <div>
    <label>
      Donation Amount:
      <input type="number" step="0.01" name="donationamount" value="<%= donationamount || '' %>" required>
    </label>
  </div>

  <div>
    <label>
      Donation Date:
      <input type="date" name="donationdate" value="<%= donationdate || '' %>" required>
    </label>
  </div>

  <div>
    <button type="submit">Save</button>
  </div>
</form>

</body>
</html>
