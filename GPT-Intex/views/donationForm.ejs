<!-- FILE: views/donationForm.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Make a Donation - Ella Rises</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <nav class="navbar">
    <div class="nav-container">
      <div class="nav-brand">🎵 Ella Rises</div>
      <ul class="nav-menu">
        <li><a href="/">Home</a></li>
        <li><a href="/donations">View Donations</a></li>
        <% if (isLoggedIn) { %>
          <li><a href="/logout">Logout</a></li>
        <% } else { %>
          <li><a href="/login">Login</a></li>
        <% } %>
      </ul>
    </div>
  </nav>
  <main class="main-container">
    <% if (error_message) { %>
      <div class="alert alert-error">❌ <%= error_message %></div>
    <% } %>
    <% if (success_message) { %>
      <div class="alert alert-success">✅ <%= success_message %></div>
    <% } %>
    <div class="container form-container">
      <h1>Support Ella Rises</h1>
      <p class="section-description">Your generous donation helps us continue empowering young women through arts and STEAM education.</p>

      <form method="POST" action="/donations/new" class="form donation-form">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        
        <p class="form-section-title">Donor Information</p>

        <div class="form-row">
          <div class="form-group">
            <label for="firstName">First Name *</label>
            <input type="text" id="firstName" name="firstName" required>
          </div>
          <div class="form-group">
            <label for="lastName">Last Name *</label>
            <input type="text" id="lastName" name="lastName" required>
          </div>
        </div>

        <div class="form-group">
          <label for="email">Email *</label>
          <input type="email" id="email" name="email" required>
        </div>

        <div class="form-group">
          <label for="phone">Phone</label>
          <input type="tel" id="phone" name="phone">
        </div>

        <p class="form-section-title">Donation Amount</p>

        <div class="form-group">
          <label for="amount">Donation Amount (USD) *</label>
          <div class="input-with-prefix">
            <span class="currency">$</span>
            <input type="number" id="amount" name="amount" step="0.01" min="0.01" required placeholder="50.00">
          </div>
        </div>

        <div class="donation-presets">
          <p class="presets-label">Quick amounts:</p>
          <button type="button" class="btn btn-small" onclick="document.getElementById('amount').value = 25;">$25</button>
          <button type="button" class="btn btn-small" onclick="document.getElementById('amount').value = 50;">$50</button>
          <button type="button" class="btn btn-small" onclick="document.getElementById('amount').value = 100;">$100</button>
          <button type="button" class="btn btn-small" onclick="document.getElementById('amount').value = 250;">$250</button>
        </div>

        <div class="form-actions">
          <button type="submit" class="btn btn-primary btn-large">Donate Now</button>
          <a href="/" class="btn btn-secondary">Cancel</a>
        </div>

        <p class="donation-note">💝 Thank you for supporting Ella Rises!</p>
      </form>

      <div class="alternative-donation">
        <p>Or donate directly through our fundraising partner:</p>
        <a href="https://givebutter.com/EllaRises" target="_blank" class="btn btn-secondary">Donate via GiveButter</a>
      </div>
    </div>
  </main>
  <footer class="footer">
    <p>&copy; 2025 Ella Rises - Empowering the future generation of women</p>
  </footer>
</body>
</html>
