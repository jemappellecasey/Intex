<!-- FILE: views/participantForm.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= participant ? 'Edit Participant' : 'Add Participant' %> - Ella Rises</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <nav class="navbar">
    <div class="nav-container">
      <div class="nav-brand">🎵 Ella Rises</div>
      <ul class="nav-menu">
        <li><a href="/dashboard">Dashboard</a></li>
        <li><a href="/participants">Participants</a></li>
        <li><a href="/logout">Logout</a></li>
      </ul>
    </div>
  </nav>
  <main class="main-container">
    <div class="container form-container">
      <h1><%= participant ? 'Edit Participant' : 'Add New Participant' %></h1>
      <form method="POST" action="<%= participant ? '/participants/' + participant.id + '/edit' : '/participants/add' %>" class="form">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        
        <div class="form-row">
          <div class="form-group">
            <label for="firstName">First Name *</label>
            <input type="text" id="firstName" name="firstName" value="<%= participant ? participant.participant_first_name : '' %>" required>
          </div>
          <div class="form-group">
            <label for="lastName">Last Name *</label>
            <input type="text" id="lastName" name="lastName" value="<%= participant ? participant.participant_last_name : '' %>" required>
          </div>
        </div>

        <div class="form-group">
          <label for="email">Email *</label>
          <input type="email" id="email" name="email" value="<%= participant ? participant.email : '' %>" required>
        </div>

        <div class="form-row">
          <div class="form-group">
            <label for="phone">Phone</label>
            <input type="tel" id="phone" name="phone" value="<%= participant ? participant.participant_phone : '' %>">
          </div>
          <div class="form-group">
            <label for="dob">Date of Birth</label>
            <input type="date" id="dob" name="dob" value="<%= participant ? participant.participant_dob : '' %>">
          </div>
        </div>

        <div class="form-row">
          <div class="form-group">
            <label for="role">Role</label>
            <input type="text" id="role" name="role" value="<%= participant ? participant.participant_role : '' %>" placeholder="e.g., Dancer, Musician">
          </div>
          <div class="form-group">
            <label for="fieldOfInterest">Field of Interest</label>
            <input type="text" id="fieldOfInterest" name="fieldOfInterest" value="<%= participant ? participant.participant_field_of_interest : '' %>" placeholder="e.g., STEM, Arts">
          </div>
        </div>

        <div class="form-row">
          <div class="form-group">
            <label for="city">City</label>
            <input type="text" id="city" name="city" value="<%= participant ? participant.participant_city : '' %>">
          </div>
          <div class="form-group">
            <label for="state">State</label>
            <input type="text" id="state" name="state" value="<%= participant ? participant.participant_state : '' %>" maxlength="2">
          </div>
          <div class="form-group">
            <label for="zip">ZIP Code</label>
            <input type="text" id="zip" name="zip" value="<%= participant ? participant.participant_zip : '' %>" maxlength="10">
          </div>
        </div>

        <div class="form-actions">
          <button type="submit" class="btn btn-primary">Save</button>
          <a href="/participants" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </main>
  <footer class="footer">
    <p>&copy; 2025 Ella Rises</p>
  </footer>
</body>
</html>
